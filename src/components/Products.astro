---
import content from '../content/pages/index.json';

const oferta = content.sections.oferta;
const ofertaCta = content.sections.ofertaCta;
const lustroSzklo = content.sections.lustroSzklo;

const frameTypes = oferta.items;
---

<section id="oferta" class="section-spacing section-padding relative" style="background-color: var(--color-bg-secondary);">
  <div class="max-w-7xl mx-auto">
    <!-- Header -->
    <div class="text-center max-w-3xl mx-auto animate-on-scroll">
      <span class="label-tag">{oferta.eyebrow}</span>
      <h2 class="mt-6 font-display text-3xl sm:text-4xl md:text-5xl tracking-tight" style="color: var(--color-text);" set:html={oferta.heading} />
      <p class="mt-6 text-base md:text-lg leading-relaxed font-body" style="color: var(--color-text-secondary);" set:html={oferta.text} />
    </div>

    <!-- Frame types grid -->
    <div class="mt-16 grid sm:grid-cols-2 gap-6">
      {frameTypes.map((frame, i) => (
        <a
          href={`/oferta/${frame.slug}/`}
          class="animate-on-scroll card-base rounded-2xl overflow-hidden group block"
          style={`transition-delay: ${i * 0.1}s; text-decoration: none;`}
          aria-label={`Zobacz wariant: ramka ${frame.size} ${frame.unit} - ${frame.name}`}
        >
          <!-- Image -->
          <div class="aspect-[3/2] overflow-hidden">
            <picture>
              <source
                type="image/webp"
                srcset={`/${frame.image.replace(/\.(jpg|webp)$/, '')}-400.webp 400w, /${frame.image.replace(/\.(jpg|webp)$/, '')}-640.webp 640w, /${frame.image.replace(/\.(jpg|webp)$/, '')}-900.webp 900w, /${frame.image.replace(/\.(jpg|webp)$/, '')}-1280.webp 1280w`}
                sizes="(min-width: 1280px) 620px, (min-width: 640px) 45vw, 90vw"
              />
              <img
                src={frame.image}
                alt={frame.imageAlt}
                width="900"
                height="600"
                class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
                loading="lazy"
                decoding="async"
              />
            </picture>
          </div>

          <!-- Content -->
          <div class="p-6 md:p-8">
            <div class="flex items-baseline gap-3">
              <span class="font-display text-4xl md:text-5xl" style="color: var(--color-accent);">
                {frame.size}
              </span>
              <span class="font-heading text-sm font-medium tracking-wider uppercase" style="color: var(--color-text-muted);">
                {frame.unit}
              </span>
            </div>
            <h3 class="mt-3 font-heading text-xl font-semibold" style="color: var(--color-text);">
              {frame.name}
            </h3>
            <p class="mt-2 text-sm leading-relaxed font-body" style="color: var(--color-text-secondary);">
              {frame.text}
            </p>
            <div class="mt-4 flex items-center justify-between gap-3">
              <span class="inline-flex items-center gap-1.5 text-xs font-heading font-medium" style="color: var(--color-accent);">
                <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M9.568 3H5.25A2.25 2.25 0 003 5.25v4.318c0 .597.237 1.17.659 1.591l9.581 9.581c.699.699 1.78.872 2.607.33a18.095 18.095 0 005.223-5.223c.542-.827.369-1.908-.33-2.607L11.16 3.66A2.25 2.25 0 009.568 3z" />
                  <path stroke-linecap="round" stroke-linejoin="round" d="M6 6h.008v.008H6V6z" />
                </svg>
                {frame.label}
              </span>
              <span class="inline-flex items-center gap-1 text-xs font-heading font-medium opacity-0 -translate-x-1 transition-all duration-300 group-hover:opacity-100 group-hover:translate-x-0" style="color: var(--color-accent);">
                {oferta.ctaLabel}
                <svg class="w-3.5 h-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                  <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
                </svg>
              </span>
            </div>
          </div>
        </a>
      ))}
    </div>

    <!-- 33-degree cut section 
    <div class="mt-20 animate-on-scroll">
      <div class="card-base rounded-2xl overflow-hidden">
        <div class="grid lg:grid-cols-2">
          
          <div class="aspect-[4/3] lg:aspect-auto overflow-hidden">
            <img
              src="img/front33mm.webp"
              alt="Front z uchwytem zacinanym na 33 stopnie - bezuchwytowy design"
              class="w-full h-full object-cover"
              loading="lazy"
              decoding="async"
            />
          </div>

          
          <div class="p-8 md:p-12 lg:p-16 flex flex-col justify-center">
            <span class="label-tag">Nowość</span>
            <h3 class="mt-5 font-display text-2xl sm:text-3xl md:text-4xl tracking-tight" style="color: var(--color-text);">
              Fronty z&nbsp;uchwytem zacinanym<br />
              <span class="text-gradient">na 33°</span>
            </h3>
            <div class="mt-6 space-y-4 text-base leading-relaxed font-body" style="color: var(--color-text-secondary);">
              <p>
                Dzięki zacinaniu pod kątem 33&nbsp;stopni możesz pozbyć się tradycyjnych uchwytów
                i&nbsp;zrezygnować z&nbsp;niewygodnego systemu tip-on. Gładka, bezuchwytowa forma
                z&nbsp;wyczuwalną fakturą drewna pod palcami.
              </p>
              <p>
                Kompatybilne z&nbsp;uchwytami korytkowych systemów Schüco i&nbsp;GTV. Front wybierasz
                z&nbsp;pełnej palety płyt laminowanych — w&nbsp;tym drewnopodobnych z&nbsp;naturalną strukturą.
              </p>
              <p>
                Wszystkie fronty z&nbsp;zacinaniem 33° są dodatkowo zabezpieczane specjalnym klejem
                odpornym na wilgoć.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>-->

    <!-- Full offer CTA -->
    <div class="mt-12 text-center animate-on-scroll">
      <a href="/oferta/" class="inline-flex items-center gap-2 btn-primary">
        <span>{ofertaCta.ctaLabel}</span>
        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
          <path stroke-linecap="round" stroke-linejoin="round" d="M13.5 4.5L21 12m0 0l-7.5 7.5M21 12H3" />
        </svg>
      </a>
      <p class="mt-3 text-xs font-body" style="color: var(--color-text-muted);">
        {ofertaCta.text}
      </p>
    </div>

    <!-- Glass & mirror note -->
    <div class="mt-8 animate-on-scroll">
      <div class="rounded-2xl p-6 md:p-8" style="background-color: var(--color-bg-tertiary); border: 1px solid var(--color-border);">
        <div class="flex gap-4">
          <div class="flex-shrink-0 w-10 h-10 rounded-xl flex items-center justify-center" style="background-color: var(--color-accent); color: white;">
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="1.5">
              <path stroke-linecap="round" stroke-linejoin="round" d="M11.25 11.25l.041-.02a.75.75 0 011.063.852l-.708 2.836a.75.75 0 001.063.853l.041-.021M21 12a9 9 0 11-18 0 9 9 0 0118 0zm-9-3.75h.008v.008H12V8.25z" />
            </svg>
          </div>
          <div>
            <h4 class="font-heading font-semibold text-sm" style="color: var(--color-text);">{lustroSzklo.heading}</h4>
            <p class="mt-2 text-sm leading-relaxed font-body" style="color: var(--color-text-secondary);" set:html={lustroSzklo.text} />
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
